version: '3.7'

networks:
  proxy:
    external: true

services:
  web:
    image: $CI_REGISTRY_IMAGE:latest
    restart: always
    environment:
      - VIRTUAL_HOST=votes.dxlb.nl,votes.dexterlabs.nl
      - LETSENCRYPT_HOST=votes.dxlb.nl,votes.dexterlabs.nl
    networks:
      - proxy
